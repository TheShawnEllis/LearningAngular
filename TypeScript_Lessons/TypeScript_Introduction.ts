// Declaring Variables: TypeScript Simple Types
let myNum: number = 14;
let myStr: string = "Hello Worlkd!";
let myBool: boolean = true;

// Any type
let myAny: any = "Can be anything";
let mySwitch: any = true;

// Arrays
let myNumArray: number[] = [1, 2, 3, 4, 5];
let myStrArray: string[] = ["My", "String", "Array"];

// For loop
for (let i=0; i < 5; i++) {
    console.log(i);
}

// Foreach Loop
for (let str of myStrArray) {
    console.log(str);
}